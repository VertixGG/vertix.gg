// Test file for ESLint spacing rules
function test() {
    const arr = [ 1, 2, 3 ];
    const obj = { key: "value" };
    
    if( true ) {
        console.log( "test" );
    }
    
    switch( 1 ) {
        case 1:
            break;
    }
}

