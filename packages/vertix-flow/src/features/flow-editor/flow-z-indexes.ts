/**
 * Defines standard z-index layers for the flow editor components.
 */
export const Z_INDEXES = {
    EDGE_DEFAULT: 1,
    NODE_DEFAULT: 10,
    EDGE_INTER_FLOW: 1000, // High z-index to render inter-flow edges above nodes
    // Add more layers as needed (e.g., selected nodes, panels)
};
